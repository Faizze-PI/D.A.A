
#all three methopds (ordered)
#1.Master Theorem
def master_theorem(a, b, k):
    return f"T(n) = O(n^{k})"

#2.Substitution Method
def substitution_method():
    return f"T(n) = O(log(n))"

#3.Iteration Method
def iteration_method():
    return f"T(n) = O(n)"
